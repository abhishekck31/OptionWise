#!/usr/bin/env node
// Placeholder for `make seed`.
//
// There is no data model, database, or ingested/sample data yet — those land in
// later TASKS.md items ("Data model + migrations", "Ingestion"). Once they exist,
// replace this script with real seeding logic (e.g. load config/categories,
// data/raw KEA PDFs or a generated is_sample=true dataset, and write to the DB).
console.log("[seed] Nothing to seed yet: no data model or database exists.");
console.log("[seed] See TASKS.md → Foundation → 'Data model + migrations' and 'Ingestion'.");
