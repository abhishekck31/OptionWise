# apps/web

OptionWise's Next.js (App Router, TypeScript, Tailwind CSS) frontend.

Part of the `optionwise` pnpm workspace — run commands from the repo root
(`pnpm dev`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, or the `make` targets
in the repo root Makefile). See the root `AUDIT.md` and `SPEC.md` for context.
