# data/docs

Put KEA rules/brochure documents here (PDFs, etc.) — used to verify
`config/categories.json` (see `BLOCKED.md`) and, later, as the RAG corpus for the AI
counsellor's RulesAgent (SPEC.md).

Empty right now — nothing has been placed here yet.
